import {Story} from '/js/story.js';

function MakeUIHandler () {
    const characterName = document.querySelector('#characterName' + instance);
    const buttons = document.querySelector('#buttons' + instance);
    const displayText = document.querySelector('#text' + instance);

    console.log(characterName);

    console.log(buttons);

    console.log(displayText);
}
